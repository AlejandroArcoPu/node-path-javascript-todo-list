export class DataManager {
  constructor(projects, tasks) {
    this.projects = projects;
    this.tasks = tasks;
  }

  addProject(project) {}

  addTask(task) {}

  removeProject(project) {}

  removeTask(task) {}
}
